# Demo Login Page

Simple standalone demo login page with Sign Up, Sign In, Forgot Password, and Show Password features.

To run:

- Open `index.html` in your browser.

Notes:

- This is a static demo. Accounts are stored in `localStorage` for demonstration only.
- No server or email sending is implemented.